streamlit==1.16.0pandas==1.5.3seo-analyzer==0.1.0  # Replace with the actual package if available, or leave it out if customcontent-analyzer==0.1.0  # Replace with the actual package if available, or leave it out if customsitemap-handler==0.1.0  # Replace with the actual package if available, or leave it out if customschema-handler==0.1.0  # Replace with the actual package if available, or leave it out if custombeautifulsoup4==4.11.1requests==2.28.1lxml==4.9.1openai  # No version constraintpyyaml==6.0